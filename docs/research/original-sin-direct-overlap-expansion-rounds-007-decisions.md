# Original Sin direct-overlap rounds 007 decisions

Boundary repair v7 approved Beltempest chunk `2584`. Roz chunks `5055` and `973` remain bounded final repairs because the shorter trims respectively leave an abrupt/unclear ending and truncate the final word.

Expansion batch 007 approved exact substitutions at chunks `2080`, `3431`, `2144`, `1618`, `2398`, `3451`, `223`, and `5198`. Doctor chunk `2398` is also approved expressive Doctor reference-bank evidence.

The following lines require one final bounded cleanup: Beltempest `2373`, Bernice `3`, Doctor `5462`, Doctor `1731`, Doctor `3116`, and Doctor `2231`. Doctor `1731` must be re-reviewed for reference-bank use after its start artifact is removed. Bernice `15` is source-blocked. Roz chunks `2175` and `426` are excluded because blind review identified Chris Cwej rather than Roz.

The expansion has 66 blind-approved substitutions beyond the original six-line pilot, for 72 approved exact substitutions total. Boundary repair v8 contains 20 candidates across eight lines. Batch 008 contains the final 16 never-attempted strict-overlap rows; 14 lines and 28 candidates reached review, Roz `811` failed both treatment gates, and Doctor `4610` was excluded because the performance says materially different words from the book.

No project Voice assignment, chunk binding, audio file, or protected project JSON was changed.
