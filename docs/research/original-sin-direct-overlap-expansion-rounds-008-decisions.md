# Original Sin direct-overlap rounds 008 and terminal v9 decisions

The completed boundary-repair v8 and expansion batch 008 reviews add six exact adaptation substitutions: Beltempest `2373`, Doctor `5462`, Doctor `1731`, Bernice `1210`, Bernice `1939`, and Doctor `2394`.

Reference-bank dispositions are separate from direct-placement approval:

- Doctor `5462`: approved expressive Doctor reference-bank evidence.
- Doctor `1731`: approved expressive Doctor reference-bank evidence, specifically preserving strong rolled-R delivery.
- Bernice `1939`: approved Bernice reference-bank evidence.
- Doctor `4888`: pending direct-placement and Doctor reference-bank re-review after final-word recovery.
- Roz `973`: not approved for either route; the final consonant remains absent after bounded in-source recovery.

The completed reviews also exclude Roz `2840` because the adaptation performance is Bernice, not Roz. Doctor `4610` remains excluded because the underlying audio does not contain the book's exact wording.

Boundary repair v9 was the final bounded repair round. It closed three strict-clean substitutions:

- Bernice `365`
- Bernice `4071`
- Doctor `4443`

Doctor `4443` is also approved as general expressive Doctor reference-bank evidence.

The user separately approved three lines for direct placement despite acknowledged minor artifacts, while explicitly rejecting them for generation or reference-bank use:

- Doctor `1801`
- Bernice `3025`
- Doctor `4907`

These are recorded as `restricted_user_accepted_artifacts`, excluded from default installation, and require explicit inclusion during any later production promotion.

Five terminal failures are retired with no further repair loop:

- Roz `5055`: final `t` in “out” remains clipped.
- Doctor `3116`: adjacent speech remains at the end.
- Roz `3016`: final-word recovery remained unusable.
- Doctor `4715`: music remains at the end.
- Doctor `4888`: “misery” remains clipped with an artifact pop; the requested reference-bank route is therefore rejected.

The complete promotion manifest supersedes the six-line pilot and records:

- 81 strict-clean direct substitutions, including the original six-line pilot;
- 3 restricted direct-placement-only substitutions;
- 84 user-approved direct placements total;
- 8 direct-adaptation reference-bank samples;
- 2 reference-only samples;
- zero untouched or repair-pending strict-overlap rows.

The complete manifest remains non-installing. No production files, chunk bindings, Voice assignments, or live reference-bank assignments were changed.
