# Original Sin direct-overlap expansion rounds 006 decisions

No Alexandria project state was changed. Written notes override nominal pass
buttons, incomplete score bundles fail closed, and every direct substitution
still requires complete 5/5 blind scores with no blocking note.

## Boundary repair v6

Approved exact substitutions:

- Chris Cwej `2979` — `5f00c22860ea0571`
- Bernice `5336` — `b7899a316d3f90b1`
- Doctor `5353` — `619b52e585c1ab95`
- Zebulon Pryce `3090` — `0cb6beadacaafc3c`

Beltempest `2746`, Chris Cwej `5120`, and Tobias Vaughn `4675` are
source- or quality-blocked after bounded repair.

## Expansion batch 006

Approved exact substitutions:

- Bernice `1401`
- Doctor `750`
- Doctor `3979`
- Doctor `561`
- Roz Forrester `3189`
- Roz Forrester `5431`

Doctor `561` is also approved expressive reference-bank evidence. Roz `3189`
is explicitly direct-placement-only and must not enter the generation reference
bank.

Beltempest `2584` needs an explicit isolation score because the exported review
omitted that field. Roz `5055` and `973` need tighter trailing trims. Bernice
`3471` is quality blocked. Beltempest `4580` is excluded because blind review
found that the selected performance does not have Beltempest's identity.

Doctor `864`, Hater of Humans `3948`, and Roz `626` are excluded from the strict
set because timed-word inspection proves that the underlying audio does not
contain the full book wording represented by the transcript. Beltempest `4587`,
Bernice `71`, Chris `1094`, and Powerless `5273` remain objective-treatment
blocked and may not be silently recycled into another broad batch.

The cumulative approved set is now 57 expansion substitutions plus the six-line
pilot, or 63 unique exact adaptation substitutions total.
